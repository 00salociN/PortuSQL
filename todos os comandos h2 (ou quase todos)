Comandos (Manipulação de Dados)

SELECT → SELECIONAR

INSERT → INSERIR

UPDATE → ATUALIZAR

DELETE → EXCLUIR

BACKUP → BACKUP / CÓPIA DE SEGURANÇA

CALL → CHAMAR

EXECUTE IMMEDIATE → EXECUTAR IMEDIATAMENTE

EXPLAIN → EXPLICAR

MERGE INTO → MESCLAR EM

MERGE USING → MESCLAR USANDO

RUNSCRIPT → EXECUTAR SCRIPT

SCRIPT → SCRIPT

SHOW → MOSTRAR

Explicit table → Tabela explícita

Table value → Valor da tabela

WITH → COM

Comandos (Definição de Dados)

ALTER DOMAIN → ALTERAR DOMÍNIO

ALTER DOMAIN ADD CONSTRAINT → ALTERAR DOMÍNIO ADICIONAR RESTRIÇÃO

ALTER DOMAIN DROP CONSTRAINT → ALTERAR DOMÍNIO REMOVER RESTRIÇÃO

ALTER DOMAIN RENAME → ALTERAR DOMÍNIO RENOMEAR

ALTER DOMAIN RENAME CONSTRAINT → ALTERAR DOMÍNIO RENOMEAR RESTRIÇÃO

ALTER INDEX RENAME → ALTERAR ÍNDICE RENOMEAR

ALTER SCHEMA RENAME → ALTERAR ESQUEMA RENOMEAR

ALTER SEQUENCE → ALTERAR SEQUÊNCIA

ALTER TABLE ADD → ALTERAR TABELA ADICIONAR

ALTER TABLE ADD CONSTRAINT → ALTERAR TABELA ADICIONAR RESTRIÇÃO

ALTER TABLE RENAME CONSTRAINT → ALTERAR TABELA RENOMEAR RESTRIÇÃO

ALTER TABLE ALTER COLUMN → ALTERAR TABELA ALTERAR COLUNA

ALTER TABLE DROP COLUMN → ALTERAR TABELA REMOVER COLUNA

ALTER TABLE DROP CONSTRAINT → ALTERAR TABELA REMOVER RESTRIÇÃO

ALTER TABLE SET → ALTERAR TABELA DEFINIR

ALTER TABLE RENAME → ALTERAR TABELA RENOMEAR

ALTER USER ADMIN → ALTERAR USUÁRIO ADMIN

ALTER USER RENAME → ALTERAR USUÁRIO RENOMEAR

ALTER USER SET PASSWORD → ALTERAR USUÁRIO DEFINIR SENHA

ALTER VIEW RECOMPILE → ALTERAR VISÃO RECOMPILAR

ALTER VIEW RENAME → ALTERAR VISÃO RENOMEAR

ANALYZE → ANALISAR

COMMENT ON → COMENTAR EM

CREATE AGGREGATE → CRIAR AGREGADO

CREATE ALIAS → CRIAR ALIAS / APELIDO

CREATE CONSTANT → CRIAR CONSTANTE

CREATE DOMAIN → CRIAR DOMÍNIO

CREATE INDEX → CRIAR ÍNDICE

CREATE LINKED TABLE → CRIAR TABELA VINCULADA

CREATE ROLE → CRIAR FUNÇÃO / PAPEL

CREATE SCHEMA → CRIAR ESQUEMA

CREATE SEQUENCE → CRIAR SEQUÊNCIA

CREATE TABLE → CRIAR TABELA

CREATE TRIGGER → CRIAR TRIGGER / GATILHO

CREATE USER → CRIAR USUÁRIO

CREATE VIEW → CRIAR VISÃO

CREATE MATERIALIZED VIEW → CRIAR VISÃO MATERIALIZADA

DROP AGGREGATE → REMOVER AGREGADO

DROP ALIAS → REMOVER ALIAS / APELIDO

DROP ALL OBJECTS → REMOVER TODOS OS OBJETOS

DROP CONSTANT → REMOVER CONSTANTE

DROP DOMAIN → REMOVER DOMÍNIO

DROP INDEX → REMOVER ÍNDICE

DROP ROLE → REMOVER PAPEL

DROP SCHEMA → REMOVER ESQUEMA

DROP SEQUENCE → REMOVER SEQUÊNCIA

DROP TABLE → REMOVER TABELA

DROP TRIGGER → REMOVER TRIGGER / GATILHO

DROP USER → REMOVER USUÁRIO

DROP VIEW → REMOVER VISÃO

DROP MATERIALIZED VIEW → REMOVER VISÃO MATERIALIZADA

REFRESH MATERIALIZED VIEW → ATUALIZAR VISÃO MATERIALIZADA

TRUNCATE TABLE → ESVAZIAR TABELA

ALTER TYPE → ALTERAR TIPO

Comandos (Outros)

CHECKPOINT → PONTO DE VERIFICAÇÃO

CHECKPOINT SYNC → PONTO DE VERIFICAÇÃO SINCRONIZADO

COMMIT → CONFIRMAR TRANSAÇÃO

COMMIT TRANSACTION → CONFIRMAR TRANSAÇÃO

GRANT RIGHT → CONCEDER DIREITO

GRANT ALTER ANY SCHEMA → CONCEDER ALTERAR QUALQUER ESQUEMA

GRANT ROLE → CONCEDER PAPEL

HELP → AJUDA

PREPARE COMMIT → PREPARAR CONFIRMAÇÃO

REVOKE RIGHT → REVOGAR DIREITO

REVOKE ALTER ANY SCHEMA → REVOGAR ALTERAR QUALQUER ESQUEMA

REVOKE ROLE → REVOGAR PAPEL

ROLLBACK → DESFAZER TRANSAÇÃO

ROLLBACK TRANSACTION → DESFAZER TRANSAÇÃO

SAVEPOINT → PONTO DE SALVAMENTO

SET @ → DEFINIR @

SET ALLOW_LITERALS → DEFINIR PERMITIR LITERAIS

SET AUTOCOMMIT → DEFINIR AUTOCOMMIT

SET CACHE_SIZE → DEFINIR TAMANHO DO CACHE

SET CLUSTER → DEFINIR CLUSTER

SET BUILTIN_ALIAS_OVERRIDE → DEFINIR SOBRESCREVER ALIAS INTERNO

SET CATALOG → DEFINIR CATÁLOGO

SET COLLATION → DEFINIR COLAÇÃO

SET DATABASE_EVENT_LISTENER → DEFINIR OUVINTE DE EVENTOS DO BANCO

SET DB_CLOSE_DELAY → DEFINIR ATRASO DE FECHAMENTO DO BANCO

SET DEFAULT_LOCK_TIMEOUT → DEFINIR TEMPO LIMITE DE BLOQUEIO PADRÃO
Comandos (Configuração / SET / SHUTDOWN)

SET DEFAULT_NULL_ORDERING → DEFINIR ORDEM PADRÃO DE NULOS

SET DEFAULT_TABLE_TYPE → DEFINIR TIPO PADRÃO DE TABELA

SET EXCLUSIVE → DEFINIR EXCLUSIVO

SET IGNORECASE → DEFINIR IGNORAR MAIÚSCULAS/MINÚSCULAS

SET IGNORE_CATALOGS → DEFINIR IGNORAR CATÁLOGOS

SET JAVA_OBJECT_SERIALIZER → DEFINIR SERIALIZADOR DE OBJETO JAVA

SET LAZY_QUERY_EXECUTION → DEFINIR EXECUÇÃO PREGUIÇOSA DE CONSULTAS

SET LOCK_MODE → DEFINIR MODO DE BLOQUEIO

SET LOCK_TIMEOUT → DEFINIR TEMPO LIMITE DE BLOQUEIO

SET MAX_LENGTH_INPLACE_LOB → DEFINIR TAMANHO MÁXIMO DE LOB LOCAL

SET MAX_LOG_SIZE → DEFINIR TAMANHO MÁXIMO DO LOG

SET MAX_MEMORY_ROWS → DEFINIR MÁXIMO DE LINHAS NA MEMÓRIA

SET MAX_MEMORY_UNDO → DEFINIR MÁXIMO DE MEMÓRIA PARA UNDO

SET MAX_OPERATION_MEMORY → DEFINIR MEMÓRIA MÁXIMA POR OPERAÇÃO

SET MODE → DEFINIR MODO

SET NON_KEYWORDS → DEFINIR NÃO-PALAVRAS-RESERVADAS

SET OPTIMIZE_REUSE_RESULTS → DEFINIR OTIMIZAÇÃO DE REUTILIZAÇÃO DE RESULTADOS

SET PASSWORD → DEFINIR SENHA

SET QUERY_STATISTICS → DEFINIR ESTATÍSTICAS DE CONSULTA

SET QUERY_STATISTICS_MAX_ENTRIES → DEFINIR MÁXIMO DE ENTRADAS DE ESTATÍSTICAS

SET QUERY_TIMEOUT → DEFINIR TEMPO-LIMITE DE CONSULTA

SET REFERENTIAL_INTEGRITY → DEFINIR INTEGRIDADE REFERENCIAL

SET RETENTION_TIME → DEFINIR TEMPO DE RETENÇÃO

SET SALT HASH → DEFINIR SALT HASH

SET SCHEMA → DEFINIR ESQUEMA

SET SCHEMA_SEARCH_PATH → DEFINIR CAMINHO DE PESQUISA DE ESQUEMA

SET SESSION CHARACTERISTICS → DEFINIR CARACTERÍSTICAS DA SESSÃO

SET THROTTLE → DEFINIR LIMITADOR / THROTTLE

SET TIME ZONE → DEFINIR FUSO HORÁRIO

SET TRACE_LEVEL → DEFINIR NÍVEL DE RASTREAMENTO

SET TRACE_MAX_FILE_SIZE → DEFINIR TAMANHO MÁXIMO DE ARQUIVO DE RASTREAMENTO

SET TRUNCATE_LARGE_LENGTH → DEFINIR TRUNCAMENTO DE GRANDES COMPRIMENTOS

SET VARIABLE_BINARY → DEFINIR VARIÁVEL BINÁRIA

SET WRITE_DELAY → DEFINIR ATRASO DE GRAVAÇÃO

SHUTDOWN → DESLIGAR / ENCERRAR

Funções Numéricas

ABS → VALOR ABSOLUTO

ACOS → ARCOSENO

ASIN → ARCSENO

ATAN → ARCTANGENTE

COS → COSSENO

COSH → COSSENO HIPERBÓLICO

COT → COTANGENTE

SIN → SENO

SINH → SENO HIPERBÓLICO

TAN → TANGENTE

TANH → TANGENTE HIPERBÓLICA

ATAN2 → ARCTANGENTE COM DOIS ARGUMENTOS

BITAND → E LÓGICO BIT A BIT

BITOR → OU LÓGICO BIT A BIT

BITXOR → OU EXCLUSIVO BIT A BIT

BITNOT → NEGAÇÃO BIT A BIT

BITNAND → NAND BIT A BIT

BITNOR → NOR BIT A BIT

BITXNOR → XNOR BIT A BIT

BITGET → OBTER BIT

BITCOUNT → CONTAR BITS

LSHIFT → DESLOCAMENTO À ESQUERDA

RSHIFT → DESLOCAMENTO À DIREITA

ULSHIFT → DESLOCAMENTO À ESQUERDA SEM SINAL

URSHIFT → DESLOCAMENTO À DIREITA SEM SINAL

ROTATELEFT → ROTAÇÃO À ESQUERDA

ROTATERIGHT → ROTAÇÃO À DIREITA

MOD → MÓDULO

CEIL → TETO (ARREDONDAR PARA CIMA)

DEGREES → CONVERTER EM GRAUS

EXP → EXPONENCIAL

FLOOR → CHÃO (ARREDONDAR PARA BAIXO)

LN → LOGARITMO NATURAL

LOG → LOGARITMO

LOG10 → LOGARITMO BASE 10

ORA_HASH → FUNÇÃO HASH ORACLE

RADIANS → CONVERTER EM RADIANOS

SQRT → RAIZ QUADRADA

PI → PI

POWER → POTÊNCIA

RAND → NÚMERO ALEATÓRIO

RANDOM_UUID → UUID ALEATÓRIO

ROUND → ARREDONDAR

SECURE_RAND → NÚMERO ALEATÓRIO SEGURO

SIGN → SINAL

ENCRYPT → CRIPTOGRAFAR

DECRYPT → DESCRIPTOGRAFAR

HASH → HASH

TRUNC → TRUNCAR

COMPRESS → COMPRIMIR

EXPAND → EXPANDIR

ZERO → ZERO

GCD → MÁXIMO DIVISOR COMUM

LCM → MÍNIMO MÚLTIPLO COMUM

Funções de Texto (String)

ASCII → RETORNAR CÓDIGO ASCII

BIT_LENGTH → COMPRIMENTO EM BITS

CHAR_LENGTH → COMPRIMENTO DE CARACTERE

OCTET_LENGTH → COMPRIMENTO EM OCTETOS

CHAR → CONVERTER PARA CARACTERE

CONCAT → CONCATENAR

CONCAT_WS → CONCATENAR COM SEPARADOR
Funções de String / Texto

DIFFERENCE → DIFERENÇA (comparação fonética)

HEXTORAW → HEX PARA RAW (converter hexadecimal em raw/binário)

RAWTOHEX → RAW PARA HEX (converter raw/binário em hexadecimal)

INSERT Function → FUNÇÃO INSERT (inserir substring)

LOWER → MINÚSCULAS

UPPER → MAIÚSCULAS

LEFT → ESQUERDA (extrair caracteres à esquerda)

RIGHT → DIREITA (extrair caracteres à direita)

LOCATE → LOCALIZAR (posição de substring)

LPAD → PREENCHER À ESQUERDA

RPAD → PREENCHER À DIREITA

LTRIM → REMOVER ESPAÇOS À ESQUERDA

RTRIM → REMOVER ESPAÇOS À DIREITA

BTRIM → REMOVER ESPAÇOS EM AMBOS LADOS

TRIM → REMOVER ESPAÇOS

REGEXP_REPLACE → SUBSTITUIR POR EXPRESSÃO REGULAR

REGEXP_LIKE → TESTAR EXPRESSÃO REGULAR

REGEXP_SUBSTR → SUBSTRING POR EXPRESSÃO REGULAR

REPEAT → REPETIR

REPLACE → SUBSTITUIR

SOUNDEX → SOUNDEX (código fonético)

SPACE → ESPAÇO (gerar caracteres de espaço)

STRINGDECODE → DECODIFICAR STRING

STRINGENCODE → CODIFICAR STRING

STRINGTOUTF8 → STRING PARA UTF8

SUBSTRING → SUBSTRING / SUBCADEIA

UTF8TOSTRING → UTF8 PARA STRING

QUOTE_IDENT → QUOTAR IDENTIFICADOR

XMLATTR → ATRIBUTO XML

XMLNODE → NÓ XML

XMLCOMMENT → COMENTÁRIO XML

XMLCDATA → CDATA XML

XMLSTARTDOC → INÍCIO DE DOCUMENTO XML

XMLTEXT → TEXTO XML

TO_CHAR → PARA TEXTO / FORMATAÇÃO EM STRING

TRANSLATE → TRADUZIR / SUBSTITUIR CARACTERES

Funções de Data e Hora

CURRENT_DATE → DATA ATUAL

CURRENT_TIME → HORA ATUAL

CURRENT_TIMESTAMP → TIMESTAMP ATUAL

LOCALTIME → HORA LOCAL

LOCALTIMESTAMP → TIMESTAMP LOCAL

DATEADD → ADICIONAR DATA

DATEDIFF → DIFERENÇA ENTRE DATAS

DATE_TRUNC → TRUNCAR DATA

LAST_DAY → ÚLTIMO DIA DO MÊS

DAYNAME → NOME DO DIA

DAY_OF_MONTH → DIA DO MÊS

DAY_OF_WEEK → DIA DA SEMANA

ISO_DAY_OF_WEEK → DIA DA SEMANA ISO

DAY_OF_YEAR → DIA DO ANO

EXTRACT → EXTRATOS / EXTRAIR PARTE DA DATA

FORMATDATETIME → FORMATAR DATA/HORA

HOUR → HORA

MINUTE → MINUTO

MONTH → MÊS

MONTHNAME → NOME DO MÊS

PARSEDATETIME → PARSE DE DATA/HORA

QUARTER → TRIMESTRE

SECOND → SEGUNDO

WEEK → SEMANA

ISO_WEEK → SEMANA ISO

YEAR → ANO

ISO_YEAR → ANO ISO

Funções do Sistema

ABORT_SESSION → ENCERRAR SESSÃO

ARRAY_GET → OBTER ELEMENTO DE ARRAY

CARDINALITY → CARDINALIDADE / QUANTIDADE DE ELEMENTOS

ARRAY_CONTAINS → ARRAY CONTÉM

ARRAY_CAT → CONCATENAR ARRAYS

ARRAY_APPEND → ADICIONAR ELEMENTO AO ARRAY

ARRAY_MAX_CARDINALITY → MÁXIMA CARDINALIDADE DE ARRAY

TRIM_ARRAY → REMOVER ELEMENTOS NULOS DO ARRAY

ARRAY_SLICE → FATIAR ARRAY

AUTOCOMMIT → AUTOCOMMIT

CANCEL_SESSION → CANCELAR SESSÃO

CASEWHEN Function → FUNÇÃO CASEWHEN (condicional)

COALESCE → COALESCE (primeiro valor não nulo)

CONVERT → CONVERTER

CURRVAL → VALOR ATUAL DE SEQUÊNCIA

CSVWRITE → ESCRITA EM CSV

CURRENT_SCHEMA → ESQUEMA ATUAL

CURRENT_CATALOG → CATÁLOGO ATUAL

DATABASE_PATH → CAMINHO DO BANCO

DATA_TYPE_SQL → TIPO DE DADO SQL

DB_OBJECT_ID → ID DO OBJETO DO BANCO

DB_OBJECT_SQL → SQL DO OBJETO DO BANCO

DB_OBJECT_SIZE → TAMANHO DO OBJETO

DB_OBJECT_TOTAL_SIZE → TAMANHO TOTAL DO OBJETO

DB_OBJECT_APPROXIMATE_SIZE → TAMANHO APROXIMADO DO OBJETO

DB_OBJECT_APPROXIMATE_TOTAL_SIZE → TAMANHO TOTAL APROXIMADO

DECODE → DECODE (condicional / mapeamento)

DISK_SPACE_USED → ESPAÇO EM DISCO USADO

SIGNAL → SINALIZAR / GERAR EXCEÇÃO

ESTIMATED_ENVELOPE → ENVELOPE ESTIMADO

FILE_READ → LEITURA DE ARQUIVO

FILE_WRITE → GRAVAÇÃO DE ARQUIVO

GREATEST → MAIOR VALOR

LEAST → MENOR VALOR

LOCK_MODE → MODO DE BLOQUEIO
Funções e Variáveis do Sistema / Sessão

LOCK_TIMEOUT → TEMPO LIMITE DE BLOQUEIO

MEMORY_FREE → MEMÓRIA LIVRE

MEMORY_USED → MEMÓRIA USADA

NEXTVAL → PRÓXIMO VALOR DE SEQUÊNCIA

NULLIF → NULO SE IGUAL

NVL2 → NVL2 (verifica se é nulo, retorna valores diferentes)

READONLY → SOMENTE LEITURA

ROWNUM → NÚMERO DA LINHA

SESSION_ID → ID DA SESSÃO

SET → DEFINIR

TRANSACTION_ID → ID DA TRANSAÇÃO

TRUNCATE_VALUE → TRUNCAR VALOR

CURRENT_PATH → CAMINHO ATUAL

CURRENT_ROLE → PAPEL ATUAL

CURRENT_USER → USUÁRIO ATUAL

H2VERSION → VERSÃO DO H2

Funções JSON

JSON_OBJECT → OBJETO JSON

JSON_ARRAY → ARRAY JSON

Funções de Tabela

CSVREAD → LER CSV

LINK_SCHEMA → LINK DE ESQUEMA

TABLE → TABELA

UNNEST → DESAGREGAR ARRAY

Funções Agregadas Gerais

AVG → MÉDIA

MAX → MÁXIMO

MIN → MÍNIMO

SUM → SOMA

EVERY → TODOS

ANY → QUALQUER

COUNT → CONTAR

STDDEV_POP → DESVIO PADRÃO POPULACIONAL

STDDEV_SAMP → DESVIO PADRÃO AMOSTRAL

VAR_POP → VARIÂNCIA POPULACIONAL

VAR_SAMP → VARIÂNCIA AMOSTRAL

ANY_VALUE → QUALQUER VALOR

BIT_AND_AGG → E BIT A BIT AGREGADO

BIT_OR_AGG → OU BIT A BIT AGREGADO

BIT_XOR_AGG → XOR BIT A BIT AGREGADO

BIT_NAND_AGG → NAND BIT A BIT AGREGADO

BIT_NOR_AGG → NOR BIT A BIT AGREGADO

BIT_XNOR_AGG → XNOR BIT A BIT AGREGADO

ENVELOPE → ENVELOPE

GCD_AGG → MÁXIMO DIVISOR COMUM AGREGADO

LCM_AGG → MÍNIMO MÚLTIPLO COMUM AGREGADO

Funções de Conjunto Binário

COVAR_POP → COVARIÂNCIA POPULACIONAL

COVAR_SAMP → COVARIÂNCIA AMOSTRAL

CORR → CORRELAÇÃO

REGR_SLOPE → INCLINAÇÃO DA REGRESSÃO

REGR_INTERCEPT → INTERCEPTAÇÃO DA REGRESSÃO

REGR_COUNT → CONTAGEM PARA REGRESSÃO

REGR_R2 → R² DA REGRESSÃO

REGR_AVGX → MÉDIA X DA REGRESSÃO

REGR_AVGY → MÉDIA Y DA REGRESSÃO

REGR_SXX → SOMA SXX (REGRESSÃO)

REGR_SYY → SOMA SYY (REGRESSÃO)

REGR_SXY → SOMA SXY (REGRESSÃO)

Funções Agregadas Ordenadas

LISTAGG → CONCATENAR LISTA

ARRAY_AGG → AGREGAR EM ARRAY

Funções de Conjunto Hipotético

RANK aggregate → RANK AGREGADO

DENSE_RANK aggregate → DENSE_RANK AGREGADO

PERCENT_RANK aggregate → PERCENT_RANK AGREGADO

CUME_DIST aggregate → DISTRIBUIÇÃO CUMULATIVA AGREGADA

Funções de Distribuição Inversa

PERCENTILE_CONT → PERCENTIL CONTÍNUO

PERCENTILE_DISC → PERCENTIL DISCRETO

MEDIAN → MEDIANA

MODE → MODA

Funções Agregadas JSON

JSON_OBJECTAGG → AGREGAR OBJETOS JSON

JSON_ARRAYAGG → AGREGAR ARRAYS JSON

Funções de Índice / Número da Linha

ROW_NUMBER → NÚMERO DA LINHA

Funções de Rank

RANK → CLASSIFICAÇÃO

DENSE_RANK → CLASSIFICAÇÃO DENSAMENTE

PERCENT_RANK → CLASSIFICAÇÃO PERCENTUAL

CUME_DIST → DISTRIBUIÇÃO CUMULATIVA

Funções LEAD / LAG

LEAD → VALOR FUTURO

LAG → VALOR ANTERIOR

Funções NTH VALUE

FIRST_VALUE → PRIMEIRO VALOR

LAST_VALUE → ÚLTIMO VALOR

NTH_VALUE → N-ÉSIMO VALOR

Outras Funções de Janela (Window)

NTILE → DISTRIBUIR EM N PARTES

RATIO_TO_REPORT → RAZÃO SOBRE O TOTAL
